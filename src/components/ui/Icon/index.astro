---
// import { Icon } from "astro-icon";
// import { ColorKey, getColor } from "../../../util/color";

// export interface Props {
//   name: string;
//   title: string;
//   color?: ColorKey;
//   hoverBackgroundColor?: ColorKey;
// }

// // get name from https://icon-sets.iconify.design/mdi/github/

// const {
//   name,
//   title,
//   color = "main",
//   hoverBackgroundColor = "blackAlpha50",
// } = Astro.props;
---

<!-- <Icon
  name={name}
  title={title}
  class={`icon-md ${color} ${hoverBackgroundColor}`}
/>

<style>
  .icon-md {
    height: 32px;
    width: 32px;
    padding: 8px;
    border-radius: 50%;
  }

  .main {
    color: var(--c-main);
  }

  .primary {
    color: var(--c-primary);
  }

  .secondary {
    color: var(--c-secondary);
  }

  .blackAlpha50:hover {
    background-color: var(--c-blackAlpha-50);
  }
</style> -->
